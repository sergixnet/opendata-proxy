{
  "name": "opendata-proxy",
  "version": "0.0.1",
  "description": "Microservice to parse json from opendata euskadi",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src -e js --exec node server.js",
    "start": "node server.js"
  },
  "engines": {
    "node": "9.4.0"
  },
  "author": "Sergio Peña",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.2",
    "request": "^2.83.0"
  },
  "devDependencies": {
    "nodemon": "^1.14.11"
  }
}
